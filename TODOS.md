# TODOS — SEPA-Eval VLA

Generated by CEO review on 2026-05-21. **Updated by Eng review on 2026-05-21. Status refreshed from implementation on 2026-06-02.**  
Source: PRD_SEPA_VLA_Eval.md (v0.2)

---

## 🔴 Blocking — Must Resolve Before Implementation

- [x] **A2: Define `CandidateTask` dataclass** ✅ Implemented in `AlphaBrain/sepa_eval/memory/schema.py`
- [x] **A5: Add `/judge` endpoint to policy server** ✅ Resolved in eng review — full spec defined (see PRD §8.3)
- [x] **Q3: Write test plan** ✅ Resolved — expanded test plan in PRD §9b (24 tests) + artifact at `~/.gstack/projects/Alchedata-sia-physicalAI-eval/fei-main-eng-review-test-plan-*.md`

---

## 🟠 High Priority — Before Phase 1 Starts

- [x] **T1: Lock in trace hook call signature** ✅ Implemented in `AlphaBrain/sepa_eval/hooks/base.py` — `TraceHook` is a context manager that accumulates per-step state and flushes via `on_episode_end()`
- [x] **T2: Lock in file store layout** ✅ Implemented in `AlphaBrain/sepa_eval/memory/eval_memory.py` — trace path is `eval_memory/{run_id}/{trace_id}.msgpack`
- [x] **T3: Add `SEPA_MEMORY_DIR` env var** ✅ Implemented in `AlphaBrain/sepa_eval/__main__.py` and `AlphaBrain/sepa_eval/memory/eval_memory.py` — default remains `./eval_memory/`
- [x] **A1: Add `BenchmarkAdapter` protocol** ✅ Implemented in `AlphaBrain/sepa_eval/hooks/base.py` with the 4 required methods: `reset()`, `step()`, `get_task_id()`, `get_scene_config()`
- [x] **A4: Implement crash-safe dual write** ✅ Implemented in `AlphaBrain/sepa_eval/memory/eval_memory.py` — `.tmp` write → atomic rename → DB commit, with `EvalMemory.fsck()` for orphan cleanup

---

## 🟡 Medium Priority — Before Respective Phase Ships

- [x] **O1: Add `sepa_eval_metrics.json`** ✅ Implemented in `AlphaBrain/sepa_eval/orchestrator/evolution_loop.py` — writes traces_written, candidates_generated, promotion_yield, critic_latency_ms, gate_timeout_count
- [x] **O2: Add `evolution_loop_log.jsonl`** ✅ Implemented in `AlphaBrain/sepa_eval/orchestrator/evolution_loop.py` — structured per-step JSONL logging
- [x] **L2: Add `promotion_evidence` JSON column** ✅ Implemented in `AlphaBrain/sepa_eval/memory/eval_memory.py` and `AlphaBrain/sepa_eval/memory/schema.py`
- [x] **D1: Add `trace_retention_days: 90` config** and `EvalMemory.prune()` method ✅ Implemented in `AlphaBrain/sepa_eval/configs/orchestrator.yaml` and `AlphaBrain/sepa_eval/memory/eval_memory.py`
- [x] **A3: Make promotion gates async** ✅ Implemented in `AlphaBrain/sepa_eval/promotion/pipeline.py` — `ThreadPoolExecutor` wrapper with timeout → deferred, not failed
- [x] **D9.2: Add `models.yaml` registry** ✅ Implemented in `AlphaBrain/sepa_eval/configs/models.yaml` and `AlphaBrain/sepa_eval/registry/models_registry.py` — YAML is the source of truth, DB is cache
- [x] **D9.3: Add `sepa_eval review list` and `sepa_eval review approve <task_id>` CLI commands** ✅ Implemented in `AlphaBrain/sepa_eval/__main__.py`
- [ ] **ENG-P1: Plan SQLite → DuckDB/Parquet migration path** — Outside voice flagged SQLite as long-term risk for binary trace volume. No action needed Phase 1-3, but design schema migration path before Phase 4 (>1 team, >10k traces).
- [ ] **ENG-P2: FAISS ANN index** — For Phase 5+ when total traces exceed 10k. DBSCAN windowing handles Phase 1-4. Add as dependency then, not now.
- [ ] **ENG-J1: Implement `/judge` endpoint in `server_policy.py`** — `SemanticCritic` already calls `POST http://{host}:{port}/judge` but the endpoint is not yet wired in `AlphaBrain/deployment/model_server/server_policy.py`. Needed before semantic critic can run against a local VLM. Unblock after: Phase 4 integration testing.
- [x] **ENG-P3: Confirm BenchmarkAdapter is version-pinned** ✅ Implemented in `AlphaBrain/sepa_eval/hooks/base.py` and benchmark hooks via `PROTOCOL_VERSION = "1.0"`
- [x] **ENG-CQ1: WAL mode** ✅ Implemented in `AlphaBrain/sepa_eval/memory/eval_memory.py` via `PRAGMA journal_mode=WAL; PRAGMA busy_timeout=5000`
- [x] **ENG-CQ2: FailureStepDetector** ✅ Implemented in `AlphaBrain/sepa_eval/mining/failure_classifier.py` — one detector per failure type with classifier fallback behavior

---
## 🚨 Pre-Release Blockers (Must Fix Before Public Release)

- [x] **PKG-1: Make `sepa_eval` independently installable** — `import sepa_eval` fails outside `AlphaBrain/`; `sepa_eval` is not declared in `AlphaBrain/pyproject.toml` as a sub-package, and there is no `[project.scripts]` entry for a `sepa-eval` CLI command. Either add `sepa_eval` to `pyproject.toml`'s `[project.scripts]` as `sepa-eval = "sepa_eval.__main__:main"` and ensure `setuptools.find_packages` discovers it, or create a standalone `pyproject.toml` under `AlphaBrain/sepa_eval/`.

- [x] **PKG-2: Declare runtime dependencies** — The following packages are imported at runtime but missing from `pyproject.toml` `dependencies`:
  - `msgpack` — trace file write/read
  - `PyYAML` — config loading (`_load_config`)
  - `openai` — `InstructionParaphrase` + `SemanticCritic` fallback
  - `requests` — `SemanticCritic` local `/judge` calls
  - `scikit-learn` — `FailureClusterer` DBSCAN
  - `jinja2` — `ReportGenerator` template rendering
  - `pyarrow` — `HardCaseExporter` lerobot parquet format (in `requirements.txt` but not `pyproject.toml`)
  - `Pillow` / `opencv-python` — critic image handling

- [x] **TST-1: Add tests for `orchestrator/evolution_loop.py`** — 0% coverage (352 lines). Minimum: one unit test per public method (`run_cycle`, `_step_evaluate`, `_step_diagnose`, `_step_generate`, `_step_validate`, `_step_monitor`, `_step_report`) using mocked sub-components.

- [x] **TST-2: Add tests for `reporting/report_generator.py` and `reporting/heatmap.py`** — 0% coverage (132 + 59 lines). Minimum: smoke test that `ReportGenerator.generate()` returns a non-empty string given a minimal mocked memory, and `build_task_model_heatmap` returns the expected matrix shape.

- [x] **TST-3: Add tests for `hooks/robocasa_trace_hook.py`** — 0% coverage (51 lines). Mirror the existing `LiberoHook` tests with a duck-typed RoboCasa env stub.

- [x] **LINT-1: Auto-fix all ruff violations** — 637 lines affected across 25 files; all are auto-fixable: run `ruff check AlphaBrain/sepa_eval/ --fix`. Key issues: unsorted imports (I001 × 12), `zip()` missing `strict=` (B905 × 8), unsorted `__all__` (RUF022 × 7), en-dash in docstrings (RUF002 × 11), unused imports (F401 × 3), bare f-string (F541 × 1).

---

## 📋 Pre-Release Nice-to-Haves

- [ ] **DOC-1: Add `sepa_eval` quickstart page** — `AlphaBrain/docs/quickstart/` has no page for SEPA-Eval. Add `sepa_eval.md` covering install, `sepa-eval run`, and reading the report.

- [ ] **DOC-2: Add API reference for `sepa_eval`** — `AlphaBrain/docs/api/` has no `sepa_eval` entry. Add at minimum: `EvalMemory`, `BenchmarkAdapter`, `EvolutionLoopOrchestrator`, `HardCaseExporter`.

- [ ] **DOC-3: Add `CHANGELOG.md` and `CONTRIBUTING.md`** — Neither exists at the repo root or under `AlphaBrain/`. Required by most OSS hosting platforms.

- [ ] **PKG-3: Align `sepa_eval.__version__`** — `sepa_eval/__init__.py` reports `0.1.0`; `AlphaBrain/pyproject.toml` reports `1.0.1`. Decide on a single version source (e.g. `importlib.metadata`) and keep them in sync.

---
## � Phase 5 — Live-Simulator End-to-End Loop

- [ ] **P5-E2E: Wire evolution loop to live simulator** — `test_evolution_loop_e2e_libero_mini` in `tests/test_integration.py` is explicitly skipped (`pytest.skip("Phase 5 not yet implemented")`). Full e2e requires: (1) live LIBERO/RoboCasa env callable from `EvolutionLoopOrchestrator`, (2) `init_state` capture in `LiberoTraceHook`, (3) `/judge` endpoint on `server_policy.py`. Unblock after OQ1 + ENG-J1 are resolved.

---

## 🟣 Demo & Tooling

- [x] **DEMO-1: Synthetic trace generator** ✅ Implemented in `demo/generate_libero_traces.py` — generates representative traces + seeds `demo/demo_eval_memory/` DB.
- [x] **DEMO-2: HTML report renderer** ✅ Implemented in `demo/render_report_html.py` — converts `report.md` to styled `report.html`; integrated in `demo/run_customer_demo.sh`.
- [x] **DEMO-3: Customer demo report artifact** ✅ `demo/report.html` rendered and committed.
- [x] **DEMO-4: Architecture overview assets** ✅ `assets/sepa_eval_architecture.png` and `assets/data_flywheel.png` added; referenced in `README.md`.

---

## �🟢 Deferred Cherry-picks (Low Priority)

- [x] **CP1: Model regression tracker** ✅ Implemented as `sepa_eval diff ModelA ModelB` in `AlphaBrain/sepa_eval/__main__.py`
  Compare per-task SR between two model checkpoints. DB query on model_task_results. ~2h.
  Unblock after: Phase 1 complete + ≥2 models registered.

- [x] **CP2: CI mode** ✅ Implemented as `sepa_eval eval --ci-mode` in `AlphaBrain/sepa_eval/__main__.py`
  Exit code 1 if new failure cluster detected or promoted task regresses vs previous eval.
  Enables GitHub Actions integration. ~1h.
  Unblock after: Phase 4 complete (promotion pipeline stable).

---

## 🔵 Open Questions (Need External Verification)

- [ ] **OQ1: LIBERO init-state replay** — Does `env.sim.get_state()` / `env.sim.set_state()` produce deterministic replay? `LiberoTraceHook` currently stores `init_state=b""` (snapshot not captured). Test with real LIBERO env before Phase 5 e2e loop commits to deterministic replay.
- [ ] **OQ2: Robocasa material API** — Does `robosuite` expose material parameter overrides at init via XML injection? `MaterialSwap` currently mutates the scene config dict only; real XML injection path is unverified. Test before Phase 3 `MaterialSwap` operator is wired to a live sim.
- [x] **OQ3: LeRobot dataset write path** ✅ Resolved — `HardCaseExporter` implements lerobot parquet format (one `episode_{i:06d}.parquet` per episode + `meta/info.json`) with graceful JSONL fallback when `pyarrow` is absent. See `AlphaBrain/sepa_eval/exporter/hard_case_exporter.py`.
- [ ] **OQ7: Circular bias mitigation** — Confirm GPT-4o-mini Vision is accessible and within budget as the default semantic critic when evaluating Qwen models.
